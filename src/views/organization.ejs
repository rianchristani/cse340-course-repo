<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>

    <h2><%= organizationDetails.username %></h2>
    <img src="/images/<%= organizationDetails.logo_filename %>" alt="<%= organizationDetails.username %> logo">

    <p>Description: <%= organizationDetails.description %></p>

    <h3>Service Projects</h3>
    <ul>
        <% if (projects && projects.length > 0) { %>
            <% projects.forEach(project => { %>
                <li><a href="/project/<%= project.project_id %>"><%= project.title %></a></li>
            <% }) %>
        <% } else { %>
            <li>No projects found for this organization.</li>
        <% } %>
    </ul>

    <% if (user && user.role_name === 'admin') { %>
        <p><a href="/edit-organization/<%= organizationDetails.organization_id %>">Edit Organization</a></p>
    <% } %>
</main>
<%- include('partials/footer') %>